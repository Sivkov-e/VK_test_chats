Тестовое задание для VK

Запуск всех тестов - `mvn test -DtestPassword={password}`, группы - `mvn test -Dtest=AttachTest -DtestPassword={password}`

Генерация allure-отчета - `allure serve allure-results`